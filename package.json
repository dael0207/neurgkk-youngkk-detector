{
  "name": "neurgkk-youngkk-detector",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "vitest run --environment jsdom",
    "check": "biome check src index.html package.json tsconfig.json biome.json netlify.toml README.md public"
  },
  "dependencies": {
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0",
    "@types/node": "^24.0.0",
    "jsdom": "^29.1.1",
    "playwright": "^1.61.1",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
